# Refactor: Prepare

You are starting a **refactor** workflow. This workflow audits the codebase,
removes AI slop and dead code, updates dependencies, and verifies no regressions.

Update dependencies: `{{update_deps}}`

Write a context summary to `{{artifact_root}}/refactor/context.md`.
