# Release: Prepare

You are starting a **release** workflow.

Target version: `{{version}}` (empty = auto-detect)
Bump type: `{{bump_type}}`
Push after release: `{{push}}`

Read the project metadata, recent git history, and current version. Write a
context summary to `{{artifact_root}}/release/context.md`.
